import React, { Component } from 'react';
import SobreContenido from '../contenido/sobreContenido';

export default class Sobre extends Component {
  render() {
    return (
      <div>
        <SobreContenido />
      </div>
    );
  }
}